enum SpecialKey {
  capsLock,
  backspace,
  plusminus,
  space,
  clear,
  hide,
  enter,
  symbols,
}

enum Capitalization {
  lowerCase,
  upperCase,
  onlyFirstLetter,
}

enum KeyBoardType {
  alphanumeric,
  numeric,
  symbols,
}
